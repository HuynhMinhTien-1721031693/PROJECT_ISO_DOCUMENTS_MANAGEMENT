# Sao chép thành file .env và chỉnh nếu cần (docker compose tự đọc .env)
# Mật khẩu SA phải đủ mạnh theo yêu cầu SQL Server (chữ hoa, thường, số, ký tự đặc biệt)

MSSQL_SA_PASSWORD=IsoDoc_Demo1!

# JWT cho API (tối thiểu ~32 ký tự cho HMAC)
JWT_SIGNING_KEY=DOCKER_DEMO_JWT_SIGNING_KEY_32_CHARS_MIN!
